name: Network Recon
on:
  push:
    branches: [main]
  workflow_dispatch:
jobs:
  recon:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run credential probe
        run: bash probe.sh
